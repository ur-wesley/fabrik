{
  "name": "fabrik-cli",
  "version": "1.0.0",
  "description": "Cross-platform CLI framework for AI workflows",
  "main": "src/cli.ts",
  "bin": {
    "fabrik": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile fabrik",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "bunx oxlint src",
    "lint:fix": "bunx oxlint --fix src && bunx oxfmt src",
    "format": "bunx oxfmt src",
    "postinstall": "node scripts/ensure-bunx-shim.cjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0",
    "@ur-wesley/ts-prelude": "latest",
    "commander": "^15.0.0",
    "valibot": "^1.4.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0"
  }
}
